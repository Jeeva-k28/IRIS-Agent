# Workspace Rules for IRIS Project

## GitHub Push Rule
- NEVER push code to GitHub automatically or after completing a task.
- ONLY execute `git push` when the user explicitly requests it in a prompt (e.g., "push to github").
